{
  "name": "react-grab-extension",
  "version": "1.0.0",
  "description": "MV3 extension via Vite to inject react-grab on all pages",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:content && npm run build:inject && npm run build:background && npm run build:popup",
    "build:content": "TARGET=content vite build",
    "build:inject": "TARGET=inject vite build",
    "build:background": "TARGET=background vite build",
    "build:popup": "TARGET=popup vite build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/chrome": "^0.1.32",
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  },
  "dependencies": {
    "react-grab": "^0.0.89"
  }
}
